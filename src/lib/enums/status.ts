export enum ClientStatus {
	active = 'Активен',
	new = 'Новый',
	inactive = 'Неактивен',
}

export enum TaskStatus {
	Todo = 'todo',
	InProgress = 'in_progress',
	Blocked = 'blocked',
	Done = 'done',
}

export enum OnlineStatus {
  ONLINE = 'online',
  OFFLINE = 'offline',
  AWAY = 'away',
  BUSY = 'busy'
}