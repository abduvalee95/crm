export enum NotificationChannel {
	EMAIL = 'email',
	PUSH = 'push',
	SMS = 'sms',
	DESKTOP = 'desktop',
}

export enum NotificationCategory {
	DEALS = 'deals',
	TASKS = 'tasks',
	MESSAGES = 'messages',
	SYSTEM = 'system',
	MARKETING = 'marketing',
}

export enum NotificationFrequency {
	INSTANT = 'instant',
	DAILY = 'daily',
	WEEKLY = 'weekly',
	NEVER = 'never',
}
